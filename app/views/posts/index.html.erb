<%= render "layouts/header" %>
<div class="cotainer mt-5">
      <div style="width: 130px; margin-left:555px; background-color:gainsboro;">
       <%= link_to '新しい順', posts_path(latest: "true"),class: "text-dark"%> |
       <%= link_to '古い順', posts_path(old: "true"),class: "text-dark" %>
       </div>
  <table class='table table-borderless mt-3'>
    <tbody>
      <% @posts.each do |postall| %>
        <tr>
            <td width="200" height="100" style="padding-left:450px;">
              <%= link_to post_path(postall.id) do %>
              <%= image_tag postall.post_image, size: '160x140' %>
              <% end %>
            </td>
            <td nowrap>
              名前：<%= postall.user.name %><br>
              日付：<%= postall.round_day %><br>
              タイトル：<%= postall.title %><br>
              ラウンド：<%= postall.round_place %><br>
              スコア：<%#= postall.total_score %><br>
              コメント：<%= postall.post_comments.count %><br>
            </td>
        </th>
      <% end %>
    </tbody>
  </table>
</div>
<div style="width: auto; margin-left:550px;"><%= paginate @posts %></div>